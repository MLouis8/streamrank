#include <vector>
#include <pair>

using pair<size_t, float> as Vertex;

class StreamGraph {
    size_t nbVertices;
    float T;
    vector<vector<pair<float, float>>> W;
    vector<vector<pair<float, float>>> E;

    void addVertex(si)
};

StreamGraph generateRDStreamGraph(size_t n, float p, float T, );

vector<Vertex> getNeighbours(Vertex v);